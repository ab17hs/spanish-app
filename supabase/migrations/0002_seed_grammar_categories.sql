-- Optional starter categories. Safe to skip if you'd rather everything come from your .docx.
-- Inserts only run for the currently-authenticated user (uses auth.uid()).

-- Common grammar categories used by the admin UI dropdown:
-- verbs, tenses, agreement, syntax, articles, prepositions, pronouns,
-- conjunctions, gender_number, accentuation, phonology, idioms

-- (No seeded data here — content comes from your imports.)
